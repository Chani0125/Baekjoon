#include <bits/stdc++.h>

using namespace std;

int num[1001];

int main(void)
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    int n, a; cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> a;
        num[a]++;
    }

    


    
    return 0;
}
